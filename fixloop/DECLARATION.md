# Fix declaration (one page) — placeholder

Filled in week 4 after the first full `floorplan bench`.

1. **Worst-performing gate and the failing number:** _tbd_
2. **Root-cause hypothesis and evidence:** _tbd_
3. **Fix to ship and the predicted number after it:** _tbd_

Before run: tag `fixloop-before`, outputs in `fixloop/before/`. After run: tag `fixloop-after`, outputs in `fixloop/after/`. Diff: `fixloop/DIFF.md` from `make fixdiff`.
