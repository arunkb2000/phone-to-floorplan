.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
cache/
results/
weights/
bench/*/debug/
*.DS_Store
# raw captures are large; tracked via LFS or fetch script (decided week 1)
data/benchmark/captures/**/*.mp4
data/benchmark/captures/**/*.mov
data/benchmark/captures/**/depth/
data/benchmark/captures/**/confidence/
data/benchmark/captures/**/rgb/
# the gate tables and both fix-loop runs ARE deliverables; only debug dumps and scratch runs are not
analysis/**/debug/
analysis/results/
analysis/bench/latest/

# The sample captures are NOT in this repository: 912 MB, and two of the videos are over GitHub's
# 100 MB per-file limit. They belong to whoever supplied them. docs/SAMPLE_DATA.md describes their
# structure and where to put them. Everything the pipeline produced from them IS committed, under
# analysis/.
data/raw/

# my own working notes: not part of the submission
private/
